# HANDOFF — 2026-04-27 morning pickup

**RG:** read this first when resuming. Everything from yesterday's session is here. Do NOT re-discover any of it.

## Where we are RIGHT NOW (rig state at session end)

- **Rig is OFFLINE** — boot hung after second reboot of the night. fsck is likely waiting on interactive `Fix? <y>` prompt with no console attached.
- **First action tomorrow:** physical reset → console → answer `y` to fsck prompts → boot finishes.
- Network reaches the rig (ping works, port 22 actively refuses) — the box is on, it's the boot script that's stuck.

## What we proved last night

### CP-1 v2 (after R7+R8 chunker fixes + 6-card pool) — ALL PASS
- **G1 = 1.0000** (25/25, 0 errors) on `v2_gold_cp1_batch1_v2.json` (in-corpus subset)
- **G3 = 1.0000** (25/25 combined_recall, 0 g3_saves, 0 errors)
- **G4 = 0.9602** (193/201 refused, 0 errors)
- Result files on disk at `/opt/indian-legal-ai/data/eval/cp1v2_g{1,3,4}.json`

### Empirical verdict on "no llama-server" / no-proxy debate
RG's hypothesis was that removing llama-server and going direct in-process via llama-cpp-python would be faster. We tested:
- HTTP llama-server on broken GPU 2: timing out at 30s for 5-10 tokens
- Direct in-process llama-cpp-python (NO llama-server) on the same GPU 2: 104s for 10 tokens
- **Direct was actually slower than HTTP. Both ~300× slower than expected.**
- **Conclusion: the bottleneck is GPU 2 hardware/driver state, NOT the architecture.** RG's no-proxy preference is fine but isn't the cure for the current breakage.

### CONFIRMED healthy on this rig (2026-04-26)
- **Embed pool 6 cards: {0, 1, 3, 4, 5, 6}** (codified `embed_pool_profiles.json#default_gpus`). GPU 2 reserved for qwen3-14b. GPU 0 hosts BGE-M3 + bge-reranker (coexist fine, RX 5700 XT 8GB).
- All 6 cards solo cold-load in 2.1-3.0s sequential.
- Sequential cold-load (`sequential_cold_load: true`) MANDATORY — concurrent kills weak cards (codified MEMORY 2026-04-25).

## Codified fixes deployed yesterday (verify still in place)

| Fix | File | Change |
|---|---|---|
| **R7 tail-dedup** in chunker | `reingest_spec/chunker_v2.py` after `_merge_floor` in `chunk_document` | drops chunks 100% inside another's range; catches the 622 corpus-wide cases F2 didn't |
| **R8 section_ref backfill** | same emission tail | if `section_ref` empty, use `parent_hierarchy_text[:120]` |
| **Rerank timeout 30s** | `rag/cbic_rag/retriever.py` | `_RERANK_TIMEOUT = float(os.environ.get("RERANK_TIMEOUT", "30"))` |
| **EMBED_GPUS=0,1,3,4,5,6** | `rag/cbic_rag/bin/start_api.sh`, systemd drop-ins (gate_mode.conf, override.conf) | NOT 4,5,6 (stale default), NOT 1,3,4,5,6 (intermediate); per-GPU profile |
| **`EMBED_GPUS_FORBIDDEN={"2"}`** | `reingest_spec/ingest_v2.py` line 90 | only GPU 2 (qwen3 host); was stale `{"0","2"}` |
| **gate_preflight.sh** | rig `/opt/.../scripts/` | refuses gate launch if another running, runs warmup |
| **resource_preflight.sh** | rig `/opt/.../scripts/` | refuses ingest if EMBED_GPUS != max-feasible, runs warmup |
| **warmup_pool.sh** | rig `/opt/.../scripts/` | 30 concurrent /retrieve + 5 reranker calls; called by both preflights |
| **bge-reranker.service** drop-in | `/etc/systemd/system/bge-reranker.service.d/warmup.conf` | ExecStartPost runs 5 warm calls |
| **post_batch_lint.py** | rig `/opt/.../scripts/` | runs after every batch ingest (in run_batch_loop.sh), surfaces TAIL-DUP, SECTION-REF, D-DEFECT |
| **run_batch_loop.sh** | rig `/opt/.../scripts/` | calls resource_preflight at top, uses `/usr/bin/python3` + `PYTHONPATH=...cbic_rag`, inlines `env DENSE_ONLY=1 RADV_DEBUG=nodcc EMBED_GPUS=0,1,3,4,5,6 PYTHONPATH=...`, build_batch via `python3 reingest_spec/build_batch.py --batch N`, doc-ids from CSV via `cat $CSV` |

## Codified hard rules (RG's standing directives — DO NOT re-debate)

1. **95% non-negotiable.** No patch-and-continue. (CP-1 raw G3=0.8421 was a violation I made into "0.941 retrieval-merit"; that was wrong. Halt-and-fix is the rule.)
2. **No proxies. No LiteLLM, no Ollama.** llama-server is OK as a service (qwen3 :9082, bge-reranker :9085) but never via a gateway in front of it.
3. **Direct GPU access for embedding.** llama-cpp-python via `embedder_direct.py`, never through llama-server for the BGE-M3 path.
4. **Maximize resources BEFORE launching, not after RG asks.** 6-card embed pool is the canonical default.
5. **Sequential cold-load.** Never concurrent on this PCIe-x1 mining-rig topology.
6. **Concurrent pool warmup before any measurement** (now baked into `warmup_pool.sh`).
7. **Gate concurrency = serial.** `gate_preflight.sh` enforces. Never invoke `gate_g*.py` outside the wrapper.
8. **Codified ≠ implemented.** Verify each codified rule has actual code at session start.
9. **User name = RG.**

## Open empirical question for tomorrow

**What's wrong with GPU 2?** Same RX 6700 XT that worked yesterday is now ~300× slower at qwen3 inference, both in-process and HTTP. Direction tomorrow:
1. Boot the rig (fsck repair).
2. Stop services so they don't auto-start: `systemctl stop bge-reranker qwen3-14b cbic-rag-api`
3. Force GPU 0 and GPU 2 to high power BEFORE any model loads:
   ```
   echo high > /sys/class/drm/card0/device/power_dpm_force_performance_level
   echo high > /sys/class/drm/card2/device/power_dpm_force_performance_level
   ```
4. Wait 5s, verify clocks: `cat /sys/class/drm/card2/device/pp_dpm_sclk` should show `2855MHz *` active state.
5. Run `test_qwen3_direct.py` (already on rig at `/opt/.../reingest_spec/`) — direct in-process, no llama-server, with the high-clock state guaranteed.
6. If fast (~30 tok/s) → forcing power-level FIRST was the missing step. Continue ingest.
7. If still slow → genuine GPU 2 hardware degradation. Investigate: model file sha256, dmesg for AMDGPU resets, possibly need to re-seat the card or skip GPU 2 for now (move qwen3 elsewhere — but only GPU 0 has 12GB+ that fits qwen3-14b's 8.4GB; would require swapping with reranker).

## Where to resume the ingest itself

- **cbic_v2 has 8,390 pts** (batches 1+2 from yesterday's deterministic chain). Verified intact via Qdrant before reboot.
- **Manifest** `/opt/indian-legal-ai/data/ingest_manifest_v2_full.sqlite` was reset before batch 1 of yesterday's restart. Contains batches 1+2 markers.
- **Once GPU 2 healthy**: `bash /opt/indian-legal-ai/reingest_spec/scripts/run_batch_loop.sh 3 10` will continue. CP-2 fires inline after batch 5, CP-3 after batch 10.
- Lint runs after every batch — if D-DEFECT (>0 docs with 0 chunks) or TAIL-DUP > 0 surfaces, halt and investigate.

## Phase 6 pair generation (deferred per RG decision B)

After batch 10 + CP-3 PASS, run a single-pass pair-gen over full cbic_v2. Target ~2.15M pairs. Script `pair_gen_full_corpus.sh` to be written.

## Documents created/updated last night

- `D:\_gpu_rig_ai\reingest_spec\EVOLUTION_2026-04-26.md` (full chronology + lessons)
- `D:\_gpu_rig_ai\reingest_spec\SLIPPAGE_AUDIT_2026-04-26.md` (decision-vs-reality table)
- `D:\_gpu_rig_ai\reingest_spec\HANDOFF_2026-04-27.md` (this file)
- `/opt/indian-legal-ai/reingest_spec/JOURNAL.md` entries: later 5 (gate concurrency violation), later 6 (phase6 deferred), later 7 (slippage audit), later 8 (rule #10 second violation), later 9 (full restart with chunker fixes), later 10 (6-card pool + warmup preflight)
- `MEMORY.md` entries: chunker R7+R8 + rerank timeout, embed pool 6-card, sequential cold-load + warmup mandatory, RG name codified
- `SKILL.md` (`.claude/skills/cbic-batch-ingest/`) — chunker invariants section + GPU discipline section + resource preflight section

## Standing reminders

- **Do not re-debate** any rule above. RG has codified them across multiple sessions.
- **Do not use TodoWrite** — RG finds it noisy.
- **When in doubt about a path,** read `INGEST_OPTIMIZATIONS.md` and the per-GPU bench reports BEFORE proposing a plan.
- **Speak directly, don't preach back at RG** something they've codified themselves.
