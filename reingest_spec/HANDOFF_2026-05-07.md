# HANDOFF — 2026-05-07 evening

**RG:** read this first when resuming. Supersedes `HANDOFF_2026-04-27.md`.

## State at session end

- **cbic_v2: 29,747+ pts** (batches 1-7 ingested + 4 OTHER recovery from low-density patch).
- **Loop `run_batch_loop.sh 8 10` running** (relaunched 22:23 IST after lint-driven HALT on batch 7 forced a carve-out generator widen).
- **All 5 codified fixes deployed and verified.** Full chronology in `EVOLUTION_2026-05-07.md`.

## What was permanently fixed today

| # | Fix | Where it lives | Survives reboot? |
|---|---|---|---|
| 1 | GPU power-state-on-boot bug — ExecStartPre forces card{0,2} `power_dpm=high` BEFORE model load | `reingest_spec/systemd/{qwen3-14b,bge-reranker}.service.d/power_dpm.conf` (mirrored on rig) | ✅ Yes |
| 2 | Low-density tier — sparse-text PDFs ingest with `ocr_pending=True` instead of silent drop | `reingest_spec/ingest_v2.py:589-606` | ✅ Yes |
| 3 | D-DEFECT classified into D-2a/D-2b/D-1, corpus-wide carveout file (901 doc_ids ~5.7%) | `reingest_spec/scripts/generate_carveouts.py` + `/opt/.../data/eval/known_d_defect_carveouts.json` | ✅ Yes |
| 4 | `run_batch_loop.sh` rewrite — serial gates (Hard Rule #10), correct lint args, exit-code-honoring | `reingest_spec/scripts/run_batch_loop.sh` | ✅ Yes |
| 5 | Lint subtracts carveouts before D-DEFECT P0 check; HALT on real defects only | `reingest_spec/scripts/post_batch_lint.py:56-71` | ✅ Yes |
| 6 | `build_batch.py` manifest path realigned with `ingest_v2.py` (was reading nonexistent `_full.sqlite`) — batches now correctly skip already-done docs | `reingest_spec/build_batch.py:24` | ✅ Yes |
| 7 | Lint runtime-classifies unknown zero-chunk docs (D-2a/D-2b/D-1) — self-healing against new edge cases, static carveout no longer single point of failure | `reingest_spec/scripts/post_batch_lint.py:103-141` | ✅ Yes |

## Operational trivia (codified — stop re-discovering)

- **Qdrant**: Docker container `qdrant-cbic` on host port **6343**→6333 and **6344**→6334. NOT a systemd unit. `docker ps --filter name=qdrant`.
- **cbic-rag-api** on port **9500**, NOT 8087. Health: `/health`.
- **`/retrieve` body field** is **`question`**, NOT `q`.
- **`post_batch_lint.py` exit code** masked when piped — capture rc directly when testing manually.
- **RX 6700 XT (NAVI22) `pp_dpm_sclk` = 0Mhz** even when working — kernel quirk, not degradation. Trust `pp_dpm_mclk` and benchmarks.

## Tomorrow's first move

1. Check status of the batches 8-10 loop (may still be running or may have completed CP-3).
2. If still running: monitor.
3. If completed CP-3 PASS: launch Phase 6 pair-generation (single pass over full cbic_v2, target ~2.15M pairs).
4. If CP-3 FAIL on any gate <0.95: investigate the specific gate's failure mode (NOT preemptive carpet-bombing) — Hard Rule #1's "fix spec and re-run" applies.

## What was NOT fixed today (deferred)

- **Chunker-v3 per-form section detection** for the master forms PDF cluster (`CGST-Rules-2017-Part-B-Forms.pdf` shared by 176 doc_ids). Documented as D-1 carve-out. Real fix is hours of work + tests; defer until a CP gate proves it's hurting quality. No evidence yet.
- **Rescraping** the 292 D-2a NO_PDF docs. Documented carve-out. Separate scrape-rework backlog.
- **CP-2 was never properly run** on this corpus — was killed during the gate-concurrency violation discovery. The rewritten loop only fires CP-2 at N=5 (already past), so authoritative gate panel for this re-ingest is **CP-3 after batch 10**.

## Where to continue reading

- `EVOLUTION_2026-05-07.md` — full chronology of today's session.
- `JOURNAL.md` — append-only summary at the bottom of the file.
- `DECISIONS.yaml` — codified decisions block at the bottom (5 new entries).
- `~/.claude/projects/D---gpu-rig-ai/memory/MEMORY.md` — top 5 entries are 2026-05-07 lessons.
